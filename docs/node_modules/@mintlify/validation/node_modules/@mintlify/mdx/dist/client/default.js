import { MDXClient as BaseMDXClient } from 'next-mdx-remote-client/csr';
import { jsx as _jsx } from 'react/jsx-runtime';

import { Popup, PopupContent, PopupTrigger } from '../ui/index.js';

export function MDXClient(props) {
  const mergedComponents = {
    Popup,
    PopupContent,
    PopupTrigger,
    ...props.components,
  };
  return _jsx(BaseMDXClient, { ...props, components: mergedComponents });
}
