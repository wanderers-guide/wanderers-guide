import { z } from 'zod';
export const errorsSchema = z
    .object({
    '404': z.object({
        redirect: z
            .boolean()
            .optional()
            .default(true)
            .describe('Whether to redirect to the home page, if the page is not found'),
        title: z.string().optional().describe('The title of the error page'),
        description: z
            .string()
            .optional()
            .describe('The description of the error page. Supports MDX formatting including links, bold, italic text, and custom components.'),
    }),
})
    .describe('Error pages configuration');
