export const isGlobalNode = (node) => {
    switch (node.type) {
        case 'language':
        case 'version':
        case 'tab':
        case 'dropdown':
        case 'anchor':
        case 'product':
            return node.data.isGlobal === true;
        default:
            return false;
    }
};
