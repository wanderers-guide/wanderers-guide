import { RehypeSyntaxHighlightingOptions } from '../plugins/index.js';
import type { SerializeOptions } from '../types/index.js';

export declare const serialize: ({
  source,
  mdxOptions,
  scope,
  parseFrontmatter,
  syntaxHighlightingOptions,
}: {
  source: string;
  mdxOptions?: SerializeOptions['mdxOptions'];
  scope?: SerializeOptions['scope'];
  parseFrontmatter?: SerializeOptions['parseFrontmatter'];
  syntaxHighlightingOptions?: RehypeSyntaxHighlightingOptions;
}) => Promise<
  import('next-mdx-remote-client/serialize').SerializeResult<
    Record<string, unknown>,
    Record<string, unknown>
  >
>;
