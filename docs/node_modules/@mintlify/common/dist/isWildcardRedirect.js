export function isWildcardRedirect(path) {
    const hasParamWildcard = /:[^*]*\*/g.test(path);
    const hasSimpleWildcard = /\*/g.test(path);
    const result = hasParamWildcard || hasSimpleWildcard;
    return result;
}
