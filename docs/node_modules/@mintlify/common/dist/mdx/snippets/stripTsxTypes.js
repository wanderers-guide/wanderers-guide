import { transform } from 'sucrase';
export const stripTsxTypes = (code) => {
    const result = transform(code, {
        transforms: ['typescript', 'jsx'],
        jsxRuntime: 'preserve',
        disableESTransforms: true,
    });
    return result.code;
};
