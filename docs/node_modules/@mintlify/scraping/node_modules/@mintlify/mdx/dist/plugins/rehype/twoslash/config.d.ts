import { type TransformerTwoslashOptions } from '@shikijs/twoslash';

export declare function getTwoslashOptions({
  linkMap,
}?: {
  linkMap: Map<string, string>;
}): TransformerTwoslashOptions;
export declare function parseLineComment(line: string):
  | {
      word: string;
      href: string;
    }
  | undefined;
