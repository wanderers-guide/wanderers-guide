export const PLAN_TIERS = ['hobby', 'trial', 'pro', 'proJul25', 'growth', 'enterprise'];
export function normalizePlan(plan) {
    if (plan === 'startup')
        return 'pro';
    return plan !== null && plan !== void 0 ? plan : 'hobby';
}
export function normalizeOrgType(org) {
    return Object.assign(Object.assign({}, org), { plan: normalizePlan(org.plan) });
}
