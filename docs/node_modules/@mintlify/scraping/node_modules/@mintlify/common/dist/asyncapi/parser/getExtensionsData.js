export const getExtensionsData = (extensions) => {
    return extensions.map((extension) => {
        return {
            id: extension.id(),
            value: extension.value(),
        };
    });
};
