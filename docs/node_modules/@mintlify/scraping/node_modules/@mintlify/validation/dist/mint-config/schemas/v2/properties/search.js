import { z } from 'zod';
export const searchSchema = z
    .object({
    prompt: z
        .string()
        .optional()
        .describe('The prompt to be displayed in the search bar placeholder'),
})
    .describe('Search display settings');
