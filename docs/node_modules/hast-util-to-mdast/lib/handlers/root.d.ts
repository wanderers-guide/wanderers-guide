/**
 * @param {State} state
 *   State.
 * @param {Readonly<HastRoot>} node
 *   hast root to transform.
 * @returns {MdastRoot}
 *   mdast node.
 */
export function root(state: State, node: Readonly<HastRoot>): MdastRoot;
export type HastRoot = import('hast').Root;
export type MdastRoot = import('mdast').Root;
export type State = import('../state.js').State;
