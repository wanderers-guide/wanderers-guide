---
title: "Find characters"
description: "When called with an API key, the caller must have a client_access grant on the requested character (set via the user UI). Without a grant, returns 403. Filtering by campaign_id only returns all characters in that campaign so members can see each other."
openapi: "POST /find-character"
---
